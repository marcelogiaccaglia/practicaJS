/* Como crear una API REST

JSON Place Holder tiene una herramienta de una API REST falsa la que debemos instalar en NodeJS 

Intalamos JSON-Server en NodeJS en forma Global

Creamos un archivo db.json

Este archivo lo podemos ejecutar con el comando en terminal de json-server -w db.json

Este archivos se levantara sobre el puerto 3000 como puerto de defecto pero si quisieramos cambiar el puerto lo podemos hacer con el comando json-server -w -p 5000 db.json*/
